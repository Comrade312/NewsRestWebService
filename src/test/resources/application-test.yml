spring:
  datasource:
    url: jdbc:h2:mem:test_news;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect
    database: h2
    show-sql: true
  flyway:
    enabled: false

